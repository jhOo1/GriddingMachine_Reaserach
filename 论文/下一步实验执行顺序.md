# 下一步实验执行顺序

更新日期：2026-08-09

当前本地证据：GriddingMachineDatasets自动测试73/73、GriddingMachine 63/63、Emerald合成输入最小烟雾5/5；生产端Windows 31编号矩阵已独立运行3次，每次29个非交互案例全部通过；`gm2`真实陆面链路已完成14个文件完整性核验和两个候选格点预实验。本文实证范围限定为Windows，产品数值与缺失值按`test/preparation/griddingmachine-v04/catergory.txt`验收。ERA5、Linux和macOS均不是投稿硬门槛，但校园网真实FTP—Zenodo访问实验保留为必要实验。

## 1. 生产流水线端到端测试（最优先、必要）

- 用临时目录生成小型二维/三维NetCDF位置编码数组。
- 覆盖`(lon,lat)`、`(lat,lon)`、`(lon,lat,ind)`、`(ind,lat,lon)`、翻转、经度平移、缩放、范围过滤和主要`GAPFILL`分支。
- 网页生成YAML直接进入`process_dataset!`；人工方向检查采用可记录的测试替身，不绕过自动结构和数值断言。
- 输出原始测试日志和案例清单；全部正式输出只能位于临时根目录。
- 进度：Windows 31编号矩阵已独立运行3次，每次29个非交互案例全部通过；HJ一名操作者已实际完成V01接受和V02拒绝，总体PASS；ELEV真实标准产品核对已完成。

## 2. 生产端溯源闭环（必要）

- 输出NetCDF记录schema版本、配置SHA-256和生产代码提交。
- 同一配置重复构建并比较科学变量与文件摘要；说明时间戳等非科学属性是否影响整文件摘要。
- 选择1个真实小型数据产品做独立数值核对。
- 进度：`ELEV_4X_1Y_V1`真实标准文件已完成底层读取、GriddingMachine整场读取和3次无损流水线核对，数组与坐标完全一致；全域无NaN，满足`catergory.txt`中“陆地不得为NaN”的要求。历史YAML迁移和原始源数据重建不属于本文硬门槛。

## 3. 直接NetCDF支持性效率实验（必要但非核心贡献）

- 使用ELEV和LAI两个内部压缩样本；`.nc`与包含同一文件的`tar.gz`各随机重复至少10次。
- 记录传输字节、首次读取时间和峰值临时磁盘；不做TOML/YAML性能比较。
- 若结果无稳定优势，只报告减少打包/解包步骤。
- 进度：ELEV和LAI的Windows暖缓存实验已完成，40次测量哈希全部一致。结论仅限定为这两个Windows暖缓存样本，不要求ERA5大文件。

## 4. Windows受控故障注入（已完成核心实验）

- 只在Windows冻结脚本和结果格式，不作跨平台声明。
- 覆盖404、超时、连接重置、错误大小、错误哈希、残留cache、全部镜像失败。
- 断言上一有效目录和正式文件不变；禁止对真实数据根目录运行清理测试。
- 进度：恢复Windows ping排序后完整包63/63测试通过；当前包级M01～M13使用固定注入ping分数完成65/65次状态断言。真实ICMP与下载表现仍由校园网FTP—Zenodo实验验证。

## 5. 真实陆面格点与模型接口（核心证据已具备）

- 在中科大校园网实际访问同一标签的机构FTP和Zenodo地址，分别记录ping、下载耗时、大小和SHA-256；这是必要实验，不得用本地HTTP替代。
- 所有缓存仅写入`D:\Emerald\GriddingMachine_Reaserach`，不对FTP、Zenodo或其他网站执行上传、覆盖或删除。
- US-NR1附近植被格点和1个非植被格点验证`read_dataset/grid_dict`真实链路。
- Emerald只做初始化和最小步进，不扩展长期模拟。
- 进度：`gm2`所需14个陆面文件（184,404,953 B）已通过大小、来源MD5和SHA-256核验。US-NR1格点成功生成34键、366日的`grid_dict`；撒哈拉候选格点按预期以“非植被”拒绝。合成`grid_weather`与Emerald最小步进5/5通过。
- 范围：真实`wd1`/ERA5可作后续扩展，不是论文软件流程验收条件；没有真实ERA5时不得声称真实气象科学正确性。

## 6. 贡献者流程复现（已完成单案例）

- 至少1名未参与开发者按冻结指南完整处理1个受控数据集；若要报告“两名参与者”，则必须实际由2人独立完成。
- 记录完成状态、口头干预点、最终NetCDF和目录断言；不做一般用户统计推断。
- 网站发布只做模拟登记；任何GitHub/Zenodo上传、删除、覆盖必须由作者明确批准并人工复核目标。
- 进度：P01在获得1次关于固定NetCDF和YAML填写方式的说明后完成，失败尝试数为0，自动验收PASS；手册已针对该中断点修订。若论文只报告“在一次干预后完成”，无需虚构或强制增加参与者；若要声称修订版指南可无帮助完成，则必须另找未执行过该任务的人复核。

## 7. 冻结与投稿（最后执行）

- 三个仓库工作区干净、测试全通过后生成本地候选tag。
- 作者确认后才允许push、建GitHub release或更新Zenodo。
- 归档环境、CSV、日志、图表脚本和数据哈希，再回填摘要、结果、讨论和结论。
