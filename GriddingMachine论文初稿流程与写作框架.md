# GriddingMachine 更新论文：研究理解、初稿流程与写作框架

> 目标期刊：《地球科学进展》  
> 当前材料：2022 年 GriddingMachine 论文、3 张手绘流程图、`后续论文流程.docx`、`GriddingMachine.jl` 与 `GriddingMachineDatasets` 当前代码快照  
> 文档性质：投稿前研究与写作路线图，不把尚未完成或尚未验证的功能写成论文结论

## 1. 一句话定位

这篇文章不应写成软件更新日志，也不应写成“利用 GriddingMachine 完成若干应用”，而应写成一篇地球系统数据基础设施方法论文：针对 2022 年版本在数据生产、发布、镜像访问、完整性保障和模型初始化方面的不足，构建并验证一个从异构地学数据到可直接用于地球系统模型的标准化 NetCDF 产品的“生产—质控—发布—发现—下载—读取—模型调用”闭环。

建议论文类型为“研究论文”，而不是“综述与评述”。决定稿件能否成立的不是修改项数量，而是能否用可重复实验回答以下 4 个科学与工程问题：

1. 新流程是否降低了地学数据标准化与贡献的门槛？
2. 新分发机制是否提高了不同网络环境和操作系统下的数据获取效率与可靠性？
3. 新质控机制是否能发现坐标、维度、缺失值和文件损坏等常见错误？
4. 新接口是否缩短了从数据标签到地球系统模型初始条件和气象驱动的路径？

### 已确认的研究边界（2026-08-07）

- `Server/Requestor` 不作为本文核心内容；正文聚焦数据生产、质控、发布、下载、读取与模型接口。
- 论文开发基线采用两个仓库的 `wyujie` 分支，后续允许继续迭代；所有正式实验仍须记录不可变的 commit ID，投稿时再冻结 release tag。
- 姜皓（Hao Jiang）为第一作者，王玉杰（Yujie Wang）为通讯作者；王玉杰的英文署名沿用 2022 年 GriddingMachine 论文。

## 2. 2022 年版本与本次升级的逻辑关系

2022 年论文将 GriddingMachine 定义为：一套标准化全球网格数据集合，以及一套可通过 Julia、Matlab、Octave、Python 和 R 下载整套数据或请求站点子集的软件。其数据发布流程为：标准化数据 → `tar.gz` 压缩包 → HTTP/FTP → `Artifacts.toml`（包含 `git-tree-sha1`、`sha256` 和 URL）→ `query_collection` / `read_LUT` / `request_LUT`。

当前升级的实质不是简单改名，而是架构重心的变化：

- 数据制品由压缩的 artifact 目录转为可直接传输和读取的 `.nc` 文件；
- 数据索引由随软件发布的 `Artifacts.toml` 转为可独立更新的 `Artifacts.yaml`；
- 单一下载地址扩展为一个标签对应多个镜像地址；
- 下载过程引入缓存文件，完整下载后才移动到公开数据目录；
- 数据读取接口统一为 `read_dataset`，并增加 `grid_dict` 与 `grid_weather`，直接服务模型初始化和气象驱动；
- 上游增加独立的 `GriddingMachineDatasets` 数据生产仓库，以 YAML 描述转换规则并生成统一 NetCDF；
- 计划通过网页/应用降低 YAML 配置与数据贡献门槛，并用合成 NetCDF 覆盖各种维度、方向和异常情况。

因此，建议把新版定义为“生命周期闭环”，而不是“下载器升级”。

## 3. 图片和 Word 中 9 项修改的整理

| 编号 | 修改主题 | 2022 年做法 | 目标做法 | 当前代码事实 | 投稿前状态 |
|---|---|---|---|---|---|
| 1 | 取消二次压缩 | NetCDF 与标识文件打包为 `tar.gz`，下载后解压 | 直接发布和读取 `.nc` | 当前索引 URL 指向 `.nc` | 已实现，需量化文件大小、下载及解压总耗时差异 |
| 2 | 多镜像与自适应下载 | 每个数据集通常一个公开地址 | 校内 FTP、本地/机构镜像与 Zenodo 并存，自动选择可用且较快的镜像 | `wyujie@51cf0fe` 的 YAML 含 1 179 个标签、1 251 个 URL，其中 72 个标签有 FTP+Zenodo 双地址；下载器按 `ping` 排序并逐个回退 | 部分实现；需改成跨平台、面向实际协议的探测方法并做校内/校外实验 |
| 3 | TOML → YAML | `Artifacts.toml` 随包管理 artifact | `Artifacts.yaml` 作为可读的数据目录与下载索引 | 已使用 YAML，字段主要为 `PATH` 与 `URL` | 已实现；需补 schema、版本字段和完整性字段 |
| 4 | 动态更新目录 | 数据目录更新与软件版本耦合 | 数据发布到 FTP/Zenodo 后更新 YAML；用户无需升级软件即可取得最新目录 | `Collector` 会从 Zenodo 获取 YAML 并重载；上游上传/登记脚本仍未接入正式模块 | 下游基本实现，上游发布闭环未完成 |
| 5 | 下载与读取 API 重构 | `query_collection`、`download_artifact!`、`read_LUT` | `download_dataset!`、`read_dataset`，支持标签或本地 `.nc`、二维/三维和可选 `std` | 新 API 已存在，`read_LUT` 仅保留为别名；README 仍展示旧 API | 核心已实现；需统一文档、兼容策略和测试 |
| 6 | 安全缓存与完整性 | artifact 系统自动进行哈希校验与解压 | 下载到 cache，成功且哈希正确后原子移动到目标目录 | “cache → move”已实现，但 YAML 没有哈希，下载器没有哈希校验 | 未完成，是投稿前硬门槛 |
| 7 | 模型参数接口 | 主要返回单个数据产品或站点子集 | `grid_dict` 聚合土壤、植被、叶片和 CO₂ 等模型参数 | 已在 `Indexer` 中实现，并与 Emerald 数据标签耦合 | 已实现；需给出可复现实例和数值正确性验证 |
| 8 | 气象驱动接口与数据贡献工具 | 无统一的模型气象驱动接口；贡献流程依赖人工 | `grid_weather` 直接形成气象驱动；网页/App 生成 YAML 或处理代码 | `grid_weather` 已实现；YAML 网页工具能预览/保存，但保存路径硬编码、依赖未完整登记 | 接口可验证；网页工具仍是原型，不宜写成成熟产品 |
| 9 | 合成数据与流水线测试 | 主要依赖真实数据和人工测试 | 自动生成不同维度顺序、坐标方向、缺失值和属性组合的 NetCDF，系统验证 pipeline | 当前 `test` 多为数据集专用脚本，没有标准 `runtests.jl` 入口，也没有完整合成矩阵 | 未完成，是证明通用性的关键实验 |

注：已确认 `Server/Requestor` 不作为本论文核心内容。尽管当前 `GriddingMachine.jl` 仍包含 `Server` 和 `Requestor`，正文不把它们列为新版贡献，仅在讨论或软件边界说明中写明“远程子集服务暂不纳入本文验证范围”。

## 4. 当前代码快照的边界

### 4.1 可以作为论文基础的部分

- 论文指定采用 `wyujie` 分支作为持续迭代的开发基线。2026-08-07 核查时，`GriddingMachine.jl/wyujie` 为 commit `715268067645b0b68ba76ffb7c1be945de048705`，`Project.toml` 标为 0.5.0，尚无 `v0.5.0` 标签；`GriddingMachineDatasets/wyujie` 为 commit `51cf0fee842c6c731b8c1836841682afec52df48`。分支会继续移动，因此论文方法、实验日志和数据可用性声明不能只写分支名，必须记录各轮实验的 commit ID，并在投稿前冻结 release tag。
- `Collector` 已支持 YAML 目录初始化、下载、更新、加载、同步、清理、数据路径查询和多 URL 下载回退。
- 下载先写入 `~/GriddingMachine/cache/<TAG>.nc`，完成后移动到 `public` 目录，可避免未完成文件直接冒充正式数据。
- `Indexer.read_dataset` 已支持整文件、周期切片、站点及站点周期读取，并可选择 `data`、`raw_data` 或 `std`。
- `grid_dict` 与 `grid_weather` 已把多个数据标签聚合为模型可消费的数据结构。
- `GriddingMachineDatasets` 主流程已实现 YAML 读取、经纬度翻转、经度范围转换、线性缩放、合法范围过滤、缺失值策略、人工方向图验收、统一保存和标签生成。

### 4.2 不能在当前稿件中表述为“已经解决”的部分

- 当前镜像探测固定调用 Unix 风格的 `ping -c 2 -W 2`，并解析 macOS/Linux 输出。Windows 会失败；部分服务器即使允许 HTTP/FTP 也可能禁用 ICMP，因此“`ping == Inf` 等于地址不可访问”并不可靠。
- 下载器未校验内容哈希；当前 `Artifacts.yaml` 也没有 SHA 字段。旧版 artifact 的自动完整性保障在取消压缩后丢失，必须恢复。
- Zenodo 目录更新通过解析网页文本寻找记录号，稳定性弱于 Zenodo API；需要测试版本更新、网络中断和页面变化情形。
- `GriddingMachineDatasets` 的部署验证、上传和 URL 检查没有在主模块中启用；FTP 上传仍是 TODO，并含服务器路径和记录号硬编码。
- YAML 生成网页把保存目录写死为服务器路径，`Oxygen` 等运行依赖未完整进入根项目环境。
- 上游仓库当前工作区有未提交修改；本文档中的目录统计已改为读取不可变的 `GriddingMachineDatasets/wyujie@51cf0fe`，但正式论文仍应使用最终 release 的统计。
- `GriddingMachine.jl` 的 README 仍展示 `download_artifact!` 等旧接口，文档与 0.5.0 开发代码不一致。
- 本轮没有成功执行 Julia 测试环境，因此本文档中的“已实现”是代码静态核查结论，不等于跨平台运行验证。

## 5. 建议的论文题目与核心贡献

### 5.1 首选题目

中文：**GriddingMachine：面向地球系统模拟的全球网格数据生产—分发—调用一体化框架**

英文：**GriddingMachine: An Integrated Framework for Producing, Distributing, and Using Global Gridded Data in Earth System Modeling**

这个题目比“GriddingMachine 版本更新”更符合《地球科学进展》的读者预期，也避免在 0.5.0 尚未正式发布时把版本号写进题名。

### 5.2 备选题目

- 面向可复现地球系统模拟的全球网格数据标准化与自适应分发：GriddingMachine 的升级与验证
- GriddingMachine 新版：全球地学网格数据的标准化生产、可靠分发与模型接口

### 5.3 可成立的 4 项贡献

只有完成相应验证后，摘要中才可以写成结果：

1. 建立 YAML 驱动的异构地学网格数据标准化与质量控制流程。
2. 建立可独立更新的目录、多镜像回退、安全缓存与哈希验证机制。
3. 统一数据下载和读取 API，覆盖标签/本地文件、二维/三维和误差变量。
4. 建立面向地球系统模型的参数字典和气象驱动接口，并用 Emerald 或等价模型完成端到端验证。

## 6. 投稿前必须完成的实验

### 6.1 实验 A：取消压缩的收益与代价

选择至少 3 类数据：二维静态、三维低时间分辨率、三维高时间分辨率。比较：

- `.nc` 与 `tar.gz` 的文件大小；
- 网络传输时间；
- 解压时间和峰值临时磁盘占用；
- 从请求开始到首次可读取数据的总时间；
- 不同 NetCDF 内部压缩级别下的差异。

不能只写“原文件已经压缩，所以无需再压缩”，需要用压缩率和端到端耗时证明。

### 6.2 实验 B：多镜像选择与跨平台可靠性

测试矩阵至少包含：

- 环境：中科大校内网、普通校外网；
- 系统：Linux、macOS、Windows；
- 镜像：校内 FTP、Zenodo；
- 指标：探测成功率、首字节时间、下载吞吐率、总耗时、回退次数、最终成功率。

建议用协议级探测代替 ICMP：HTTP(S) 使用 `HEAD` 或小范围 `Range GET`，FTP 使用连接/文件信息查询。镜像选择可先按“可达性”过滤，再按近期实测吞吐或连接耗时排序。

### 6.3 实验 C：中断与损坏注入

主动构造以下情况：

- 下载到 25%、50%、75% 时中断；
- 服务器返回 404、超时或连接重置；
- 下载文件截断；
- 随机翻转文件字节；
- 第一镜像失败、第二镜像成功；
- 缓存目录已有同名残留文件。

验收标准：损坏文件不能进入正式目录；哈希不一致时必须删除或隔离缓存并尝试下一镜像；最终错误信息应指出失败阶段和镜像。

### 6.4 实验 D：动态目录更新

在不升级 Julia 包的前提下，发布一个测试标签并更新 Zenodo 中的 `Artifacts.yaml`，验证客户端能发现新记录、刷新本地目录并下载新数据。记录目录更新时间、失败恢复和旧版目录兼容性。

### 6.5 实验 E：合成 NetCDF 流水线矩阵

自动生成最小数据集，组合覆盖：

- 维度：`(lon, lat)`、`(lat, lon)`、`(lon, lat, ind)`、`(ind, lat, lon)`；
- 纬度：南→北、北→南；
- 经度：`-180~180`、`0~360`、正向和反向；
- 数值：缩放、偏移、越界值、填充值、NaN；
- 变量：仅 `data`、`data + std`；
- 配置：合法 YAML、缺字段、错误类型、数组长度不一致；
- 分辨率与覆盖：全球规则网格、非支持分辨率、非全球区域。

每个案例必须有预期输出，自动检查维度顺序、坐标、数值、属性、变更记录和失败类型；人工方向图只作为补充验收，不能代替自动断言。

### 6.6 实验 F：模型端到端案例

选择 2~3 个有代表性的站点或小区域，通过标签调用 `grid_dict` 与 `grid_weather`，完成：

1. 参数与驱动数据自动下载；
2. 站点数据读取；
3. Emerald 初始化；
4. 一段固定时间窗模拟；
5. 与手动准备输入的结果对照。

至少报告准备代码行数、人工步骤数、总准备时间、输入变量完整率和关键模拟输出误差。文章重点是数据到模型的接口价值，不需要把模型科学结果扩展成另一篇机理研究。

## 7. 建议图表

### 图

1. **总体架构图**：原始地学数据 → YAML/网页配置 → 标准化与质控 → NetCDF → FTP/Zenodo → 动态 `Artifacts.yaml` → Collector → Indexer → `grid_dict`/`grid_weather` → 地球系统模型。
2. **2022 版与新版流程对比图**：突出 `tar.gz/TOML/artifact` 与 `.nc/YAML/multi-mirror/cache+hash` 的变化。
3. **数据生产与质量控制图**：维度调整、方向转换、缩放、范围过滤、缺失值、自动检查与人工方向图。
4. **性能与可靠性结果图**：压缩与否、多网络、多操作系统、多镜像的耗时和成功率。
5. **端到端案例图**：站点或区域的参数、气象驱动和一个模型输出示例。

### 表

1. 2022 版与新版功能对照表。
2. GriddingMachine 标准 NetCDF 变量、维度和元数据要求。
3. 合成数据测试矩阵与通过情况。
4. 下载性能、错误注入和跨平台结果。
5. 代码、数据、版本、许可证与永久链接清单。

## 8. 符合《地球科学进展》定位的正文骨架

### 题名页

- 中文题名、英文题名；
- 作者姓名和顺序；
- 中文/英文单位、城市和邮编；
- 第一作者简介；
- 通讯作者及邮箱；
- 基金项目名称和编号。

作者栏建议先保留为：

> **姜皓（Hao Jiang）**^1，**[其他实际贡献作者]**^1，**王玉杰（Yujie Wang）**^1*  
> 1. 中国科学技术大学[请核实二级单位正式名称]，安徽 合肥 [邮编]  
> * 通信作者：王玉杰，[老师邮箱]

你为第一作者、老师为通讯作者没有格式问题，但其他作者必须依据实际贡献确定，不能仅按代码提交记录推断。

### 摘要

采用“背景/问题—方法—定量结果—结论与意义”的单段报道性摘要。当前阶段只能先写目的和方法，结果句必须等待实验数据。不要写“显著提升”“高效”“可靠”等无数字支撑的评价词。

建议关键词：

> 地球系统模型；全球网格数据；数据标准化；NetCDF；科学数据基础设施；开放科学

### 1 引言

1. 地球系统模型对多源、全球、时空异质数据的需求；
2. 数据发现、格式统一、质量控制、可靠下载与模型接口的共同困难；
3. 2022 年 GriddingMachine 的贡献；
4. 旧版在压缩制品、单点存储、目录耦合、贡献流程和模型直连方面的不足；
5. 本文目标和贡献。

### 2 系统设计与方法

#### 2.1 总体架构和数据生命周期

定义生产端 `GriddingMachineDatasets` 与消费端 `GriddingMachine.jl` 的职责边界。

#### 2.2 数据标准与 YAML 驱动的生产流水线

写清 NetCDF 维度、坐标、变量、单位、缺失值、误差和 provenance/change log。

#### 2.3 质量控制与数据登记

包括自动结构检查、数值检查、方向图人工验收、标签生成、版本/修订号、上传与目录登记。

#### 2.4 动态目录与多镜像分发

包括 YAML 独立更新、镜像可达性/性能探测、失败回退、缓存和哈希验证。

#### 2.5 统一读取与模型接口

介绍 `download_dataset!`、`read_dataset`、`grid_dict`、`grid_weather`；旧接口只放兼容性说明。

### 3 验证设计

对应第 6 节实验 A~F，事先写清机器、系统、网络、Julia/Python 版本、数据标签、重复次数、统计指标和代码提交号。

### 4 结果

#### 4.1 数据生产和质控覆盖率

#### 4.2 取消压缩的端到端性能

#### 4.3 多镜像的跨网络与跨平台表现

#### 4.4 中断和损坏情况下的数据完整性

#### 4.5 模型初始化与气象驱动案例

### 5 讨论

- 新版相对 2022 年版本的实质进展；
- 对地球系统数据 FAIR 使用和可复现建模的意义；
- 与通用数据仓库、Julia artifact 机制和模型专用输入流程的区别；
- 局限：当前主要面向规则经纬网和特定 NetCDF 约定、人工验收仍存在、镜像维护成本、其他语言客户端同步、远程子集服务暂缓；
- 下一步：schema、自动发布、内容寻址、区域/非规则网格和社区治理。

### 6 结论

用 3~5 条可由结果直接支持的结论收束，不重复背景。

### 文末声明

- 数据与代码可用性；
- 作者贡献（建议使用 CRediT）；
- 利益冲突；
- 基金与致谢；
- AI 工具使用说明。

## 9. 初稿推进流程与退出条件

### 阶段 0：冻结研究边界

任务：正文只写生产、发布、下载、读取与模型接口，`Server/Requestor` 不作为核心；开发基线使用 `wyujie` 分支，投稿前冻结 release tag。  
产物：一页 scope、功能清单、作者贡献初表。  
退出条件：已确认核心范围、开发分支、第一作者与通讯作者；待补其他作者及贡献后完成本阶段。

### 阶段 1：代码收口

任务：完成跨平台镜像探测、哈希字段与验证、Zenodo API、发布模块接入、配置去硬编码、README/API 同步、正式自动测试入口。  
产物：候选发布分支、CI、变更日志。  
退出条件：Linux/macOS/Windows 核心测试通过，无依赖人工服务器路径。

### 阶段 2：实验预注册式设计

任务：先固定样本、网络环境、指标、重复次数、故障注入和统计方法，再运行实验。  
产物：`experiment_protocol.md`、机器与软件环境表、原始结果目录。  
退出条件：每项摘要候选结论都有对应指标。

### 阶段 3：运行实验并冻结版本

任务：执行 A~F；保存日志、CSV、图形脚本和随机种子；给代码与数据打 release tag/DOI。  
产物：结果表、图 1~5、可复现实验脚本、版本永久链接。  
退出条件：所有论文数字可以从脚本一键重建。

### 阶段 4：写“方法和结果”初稿

顺序建议：图表 → 方法 → 结果 → 讨论 → 引言 → 结论 → 中英文摘要。  
原因：先锁定证据，能避免摘要和引言把尚未验证的功能写得过满。  
产物：完整中文初稿 v0.1。  
退出条件：每个结论能指向图、表、测试或代码版本。

### 阶段 5：老师内审与外部可用性测试

任务：请没有参与开发的组内同学按文档贡献一个小型数据集，并记录卡点；老师审科学定位和作者贡献。  
产物：可用性记录、修订稿 v0.2、作者确认表。  
退出条件：新用户能仅凭文档完成“配置—处理—验证—登记—读取”。

### 阶段 6：按期刊格式定稿

任务：补中英文题名/摘要/关键词、单位和作者信息、基金、参考文献、数据代码声明、AI 声明；逐图检查地图合规。  
产物：投稿稿、无作者信息的送审稿（若系统要求）、图件源文件、补充材料、投稿信。  
退出条件：与投稿系统最新模板逐项核对完毕。

## 10. 《地球科学进展》要求对本稿的直接影响

根据期刊官网及可检索到的投稿说明，当前写作至少要遵守：

- 稿件应具有科学性、创新性和时效性，论点明确、数据可靠；因此不能以功能列表代替实验结果。
- 摘要应具有独立性，覆盖目的、方法、主要结论；英文摘要应充分反映核心方法和内容。
- 使用国家法定计量单位。
- 若图中含中国地图，应使用自然资源部标准地图并标注审图号；任何边界修改需要重新送审。系统架构图、全球规则网格示意若不表达行政边界，也应避免不必要的地图底图。
- 期刊鼓励数据论文提交关联数据；本稿应同时公开处理配置、合成测试数据、实验结果和绘图代码，并给出 DOI 或永久链接。
- 参考文献采用正文顺序编码的期刊体例；中文文献按期刊已发表文章的方式提供英文对照信息。
- 期刊 2024 年 12 月发布了 AI 生成内容政策：AI 不能署名；若用 AI 收集/分析数据、制作图件、生成程序或文本润色，必须在数据来源、方法或结语中说明工具、过程和贡献。本项目已使用 Codex 辅助材料梳理和写作框架，因此投稿稿必须如实披露，并由全部作者核查和承担责任。

字体、字号、篇幅、图像分辨率和投稿文件拆分方式可能随模板更新；正式投稿前应从官网投稿系统下载当期模板再次核对，不采用商业投稿网站的二手格式作为最终依据。

## 11. 摘要占位稿（等待结果后替换方括号）

> 全球陆面与地球系统模型依赖来源多样、格式异构的网格数据，数据标准化、质量控制、稳定分发及模型适配仍需要大量人工操作。针对 GriddingMachine 2022 年版本中压缩制品、单一数据源、目录更新与软件版本耦合以及数据贡献流程不完整等问题，本文构建了覆盖数据生产、验证、发布、下载、读取和模型调用的新版 GriddingMachine 框架。该框架采用 YAML 描述异构数据的坐标、维度、数值转换与缺失值策略，将数据统一为标准 NetCDF；通过可独立更新的数据目录、多镜像选择、安全缓存与内容哈希保障数据获取；并提供统一读取、模型参数字典和气象驱动接口。基于[数据集数量]个真实数据集、[合成案例数量]个合成 NetCDF 案例以及 Linux、macOS 和 Windows 上的跨网络实验，结果表明：[填写标准化正确率]；[填写下载时间/成功率提升]；[填写故障注入拦截率]；[填写模型输入准备效率]。新版框架将 GriddingMachine 从数据下载工具扩展为面向地球系统模拟的可复现数据生命周期基础设施，为全球网格数据的共享、维护和模型复用提供了统一路径。

这段目前只能作为结构模板，方括号没有实验结果前不得删除。

## 12. 下一轮需要你和老师确认的信息

已确认：使用 `wyujie` 分支持续开发；`Server/Requestor` 不作为核心；姜皓（Hao Jiang）为第一作者；王玉杰（Yujie Wang）为通讯作者。

仍需确认：

1. 中国科学技术大学正式二级单位、邮编和王玉杰老师通讯邮箱。
2. 其他作者及其实际贡献；原 2022 年共同作者是否参与本次升级需要由老师决定。
3. 基金项目名称与编号。
4. 投稿时使用的 release tag、Zenodo DOI 和数据许可；分支名不能替代可复现版本号。
5. 可用于跨平台测试的 Windows、macOS、Linux 机器及校内/校外网络条件。
6. Emerald 端到端案例选用哪些站点、年份和输出指标。

## 13. 主要资料

- 2022 年原论文：[GriddingMachine, a database and software for Earth system modeling at global and regional scales](https://www.nature.com/articles/s41597-022-01346-x)
- GriddingMachine 代码仓库：[CliMA/GriddingMachine.jl](https://github.com/CliMA/GriddingMachine.jl)
- 《地球科学进展》官网：[Advances in Earth Science](https://www.adearth.ac.cn/)
- 期刊 AI 政策：[《地球科学进展》编辑部关于 AI 生成论文内容的政策声明](https://www.adearth.ac.cn/CN/column/item82.shtml)
- 期刊数据关联倡议可由官网“通知公告”进入：[通知公告](https://www.adearth.ac.cn/CN/news/folder.do?folderId=1)
